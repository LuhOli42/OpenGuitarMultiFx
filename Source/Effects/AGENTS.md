# Effects

## Purpose
Owns: every stage of the signal chain — `EffectProcessor` (the base contract) and each concrete pedal/NAM/cab subclass (`GateProcessor`, `CompressorProcessor`, `OverdriveProcessor`, `NAMProcessor`, `IRLoaderProcessor`), plus `EffectRegistry` (factory + display-name mapping) at `Source/EffectRegistry.{h,cpp}`.
Does not own: chain ordering/execution (`Source/Engine/SignalGraph`), who owns processor instances or the UI around them (`Source/UI/MainComponent`), or realtime-swap machinery itself (`Source/Engine/DeferredReclaimer.h` — Effects only *uses* it).

## Code Map

### Find It Fast
| Looking for... | Go to |
|----------------|-------|
| The contract every processor implements | `EffectProcessor.h` |
| Factory (name → processor), display-name ↔ registry-key mapping | `Source/EffectRegistry.{h,cpp}` |
| NAM inference wrapper (3 roles: Amp/Amp+Cab/Pedal, same class) | `NAMProcessor.{h,cpp}` |
| Cab/Reverb convolution (2 roles, same class) | `IRLoaderProcessor.{h,cpp}` |
| Shared attack/release detector (used by Gate + Compressor) | `EnvelopeFollower.h` |
| Effect icon glyphs, category colours, which glyph each processor uses | `docs/icons/AGENT-icon-notes.md` (read before adding any new effect's icon) |

### Key Relationships
- `Effects/` → `Engine/DeferredReclaimer.h`: NAM models and IRs use the same atomic-swap pattern as `SignalGraph` itself.
- `SignalGraph`/`AudioEngine` never know concrete `Effects/` types — everything goes through `EffectRegistry`. Don't add special-casing for a specific effect type in `SignalGraph` (see root AGENTS.md).

## Public API
| Export | Used By | Change Impact |
|--------|---------|---------------|
| `EffectProcessor` (virtual contract) | Every concrete processor, `SignalGraph`, UI | Every subclass must implement the full contract — no partial exceptions (see root AGENTS.md code conventions) |
| `EffectRegistry::registerType/create/displayNameForKey/keyForDisplayName` | `MainComponent` (Add-effect menu, preset save/load) | `displayNameForKey`/`keyForDisplayName` are built once by *probing* a real instance's `getName()` at `registerType()` time — this is the single source of truth for the Add-effect menu AND preset serialization, so they can't drift apart. Don't hardcode display names elsewhere. |
| `EffectProcessor::wantsModelFile()`/`loadModelFile()` | Generic UI "Load model..." action | False/no-op default; true only for NAM/IR processors — lets UI code avoid `dynamic_cast` |

## Entry Points
| Task | Start Here |
|------|------------|
| Add a new effect processor | See "Adding a new effect processor" under Patterns below |
| Understand the base contract | `EffectProcessor.h` |
| Wire a processor into TONE3000 download routing | `../Tone3000/GearRouting.h` (see `Source/Tone3000/AGENTS.md`) |
| Add a new physically-modelled (circuit-accurate, not neural) pedal/amp | Read `docs/circuits/CircuitFamilies.md` FIRST (what's already built, what's reusable), then the closest existing `docs/circuits/*.md` for technique |

## Decisions
| Decision | Why | Rejected |
|----------|-----|----------|
| One `NAMProcessor` class fills 3 chain roles (Neural Amp / Neural Amp + Cab / Neural Pedal) | Inference engine doesn't care which — only the loaded `.nam` file differs; split is purely so TONE3000 search stays scoped to one gear at a time | 3 separate processor classes |
| One `IRLoaderProcessor` class fills 2 roles (Cab / Reverb) | Same convolution operation underneath; only typical IR length and chain position differ | 2 separate processor classes |
| `nam_core` built as a CMake `OBJECT` library, never `STATIC` | NAM registers each architecture (LSTM, WaveNet...) into `get_dsp()`'s factory via static-initializer side effects with no directly-referenced symbol — a real `.a` archive silently dropped those translation units at link time (confirmed: `get_dsp()` threw "No config parser registered for architecture: LSTM") | `STATIC` library |
| Eigen pinned to an exact commit (not the `3.4.1` tag) | The tagged release is missing `Eigen::placeholders::lastN`, which `NAM/lstm.h` needs; pinned commit is the exact one NAM's own submodule vendors (verified via GitHub API) | The obvious tagged release |
| `NAMProcessor`/`IRLoaderProcessor` override `getState()`/`setState()` | Base class only serializes float params; presets need to restore *which* model/IR is loaded, not just gain (fixed in commit `921e0e9` — previously a preset couldn't actually restore the loaded sound) | Relying on the base class default |
| `drawIcon()` glyphs match the user's reference sheet exactly, re-checked per icon rather than assumed | Two earlier passes guessed shapes from memory/description and got them wrong in both directions (chip-vs-amp-head for NAM roles, distinct-vs-shared for the same 3 roles) before a 2026-09-11 pass read the actual image directly and got user approval on a full 68-glyph prototype first — see `docs/icons/AGENT-icon-notes.md` | Inventing a consistent-looking icon language without the reference |
| `drawIcon()` renders an embedded `Assets/Icons/*.svg` via `juce::Drawable` (see `IconKit.h`), not hand-written `juce::Path` calls | Even after the glyphs above were approved, hand-transcribing them into `Graphics`/`Path` calls introduced its own drift per icon — stroke width hardcoded in pixels instead of scaled to the icon's real on-screen size, `drawLine()`'s flat caps vs. the approved rounded ones, wrong inset/pin-length ratios on the NAM chip. Embedding the literal approved SVG removes that whole class of bug — same reasoning as embedding Sora instead of hoping a system font looks similar | Continuing to fine-tune hardcoded `Path` coordinates by eye |
| Phase 2 (`DelayProcessor`, `ReverbProcessor`) ships one representative processor per category, not all 9 Delay/10 Reverb sheet variants | Matches how Phase 1 shipped Gate/Compressor/Overdrive rather than every Dynamics/Drive variant on the sheet; building all 19 DSP algorithms at once with no real-world testing each would be exactly the "half-finished implementation" the project avoids | Building every named variant from the icon sheet immediately |
| `ReverbProcessor::getName()` returns `"Ambient"`, not `"Reverb"` | `IRLoaderProcessor`'s `"Reverb"` chain role (IR convolution) already owns that display name; `EffectRegistry`'s display-name↔key lookup (which presets rely on) needs every registered type's name to be unique, so the new algorithmic reverb takes the sheet's "Ambient" glyph/name instead | Naming it "Reverb" and silently overwriting the IR-based one's registry entry |
| CPU pass 2026-09-13, targeting genuine per-sample savings for the eventual ARM target, not a rewrite: (1) every ring-buffer wraparound of the form `(x + 1) % length` across every delay/comb/allpass/grain-shifter processor replaced with `if (++x >= length) x = 0;` (integer division is markedly more expensive than an almost-always-correctly-predicted branch on Cortex-A-class cores); (2) `MultiTapDelayProcessor`/`DualDelayProcessor` hoist their read-position/fraction math (channel-independent) out of the per-channel loop instead of recomputing it per channel; (3) `AnalogDelayProcessor`/`TapeDelayProcessor`'s feedback-saturation `std::tanh(driveConstant)` denominator (invariant for the process's whole life) computed once via a function-local `static const` instead of every sample; (4) `NAMProcessor`/`GateProcessor`/`CompressorProcessor`/`TremoloProcessor`/`PlateReverbProcessor`/`GatedReverbProcessor`/`HoldProcessor` cache `getWritePointer()`/`getReadPointer()` once per channel per block instead of going through bounds-checked `getSample()`/`setSample()` per element; `NAMProcessor`'s output-gain multiply also collapsed from once-per-channel to once, then `copyFrom()` per channel; (5) `EnvelopeFollower::setAttackTime()`/`setReleaseTime()` skip recomputing their `std::exp()` coefficient when the caller passes the same value as last time (Gate/CompressorProcessor call both unconditionally every block) | Explicit user request ("tenta optimizar... pra gastar bem pouco processador") ahead of the Phase 6 ARM port; survey (2 independent read-only passes) found these as the concrete, measurable per-sample waste, as opposed to already-correct patterns (parameter/coefficient hoisting was fine almost everywhere) | The bigger architectural move the same survey flagged — consolidating the ~10 duplicated fractional-delay-line/comb/grain-shifter struct definitions into one shared class — deferred to the user's separately-requested, not-yet-started "revisit all effects" pass, since it's a maintainability win more than a CPU one and touches every delay/reverb/pitch file at once; also deferred: fast `tan()`/`pow()` approximations for LFO-modulated coefficients (findings 6/7), which need audible/measured accuracy validation before swapping in, not a mechanical change like the rest of this pass |
| ms/BPM tempo sync (2026-09-13) lives on the base `EffectProcessor` as a generic `registerTempoSyncParam()`/`TempoSyncBinding` mechanism (`TempoSync.h` holds the shared subdivision table), not per-effect logic -- a subclass just calls `registerTempoSyncParam (timeMs)` once its param exists. Registered on every genuine time-in-ms param: `DelayProcessor`/`AnalogDelayProcessor`/`TapeDelayProcessor`/`PingPongDelayProcessor`/`MultiTapDelayProcessor`/`ReverseDelayProcessor`'s Time, `DualDelayProcessor`'s Time A + Time B, `HoldProcessor`'s Capture, `GatedReverbProcessor`'s Hold. The audio thread stays completely unaware sync exists: `updateTempoSyncedParams(bpm)` runs on the control thread only (`MainComponent`'s UI timer, fed `FooterBar::getBpm()`), recomputes ms from (bpm, subdivision), and pushes it through the parameter exactly like a UI-driven knob drag -- `process()` only ever reads the param's current ms value, same as before this feature existed | Per explicit user request ("qualquer efeito com o tempo tem q ter a opcao de usar o tbm em ms, ou subdivisoes musicais") -- a generic base-class mechanism covers every current and future time-ms param for free, the same reasoning as `ParameterPanel`'s generic knob-builder covering every effect without per-effect UI code | A per-effect BPM-aware `process()` (would cross the realtime boundary for no reason -- BPM only changes on a human tapping a button, nowhere near audio-rate) or hardcoding the synced param list into the UI layer (would need updating in two places every time a new time-ms param is added) |
| Tempo-synced ms is clamped into the param's own `NormalisableRange` before being pushed | A whole note at a slow BPM can compute to more ms than a short delay's max (e.g. `TapeDelayProcessor`'s 2000ms ceiling) -- clamping to what the knob could already reach by hand is safe and unsurprising; letting an out-of-range value through would violate every processor's own declared range | Silently misbehaving on out-of-range subdivisions, or forbidding sync on params with a low ceiling |
| `DynamicCabProcessor` (2026-09-13) runs TWO independently-loaded IRs (own `DeferredReclaimer<juce::dsp::Convolution>` each) in parallel and crossfades the outputs, covering both "static multi-mic blending" (Blend knob, Dynamics=0) and "dynamic/level-dependent convolution" (Dynamics>0, blend pushed toward IR B by a block-level input-envelope reading) in one processor rather than two | Both were requested together and are the same DSP underneath (two convolutions + a crossfade) -- splitting them into separate processors would mean either duplicating the whole convolution/loading machinery or awkwardly wiring one to depend on the other. With only one IR loaded it behaves exactly like a single-IR `IRLoaderProcessor::Cab`, so it's never a regression even if you never touch Blend/Dynamics | Two separate processors (`MultiIRCabProcessor` + a `DynamicConvolutionProcessor` reading its output) -- more chain slots and UI for what's fundamentally one signal-flow idea |
| Level-to-blend mapping for Dynamics is this project's own simple linear design (`effectiveBlend = blend + dynamics * envelopeLevel`, clamped), not a port of any existing algorithm | Research (see project chat history 2026-09-13) found real commercial precedent (Two Notes' DynIR) for the *idea* but no open-source reference implementation to work from -- the technique is documented, the exact curve isn't, so this is an honest from-scratch, tunable-by-ear design rather than a claimed reproduction of anyone else's DSP | Overclaiming a specific commercial product's algorithm was reproduced |
| `EffectProcessor::loadModelFile()` gained a second `slotIndex` parameter (default 0) and two new hooks, `getModelSlotCount()`/`getModelSlotName()` | `DynamicCabProcessor` needs to load into one of TWO independent IR slots; every existing single-file processor (NAM, `IRLoaderProcessor`) needed a mechanical one-line update (add the unused param) to keep overriding correctly, and gets `getModelSlotCount() == 1` for free from the base default, so `ParameterPanel`'s existing Browse/Search flow needs no slot picker for them | A `DynamicCabProcessor`-specific pair of load methods (`loadIrA()`/`loadIrB()`) -- would need its own bespoke UI in `ParameterPanel` instead of reusing the existing generic Browse/Search buttons every other model-loading processor already has |
| `chowdsp_wdf` (2026-09-13) vendored via `cmake/ChowdspWDF.cmake`, no `SOURCE_SUBDIR` workaround needed unlike `nam_core` | Its own top-level `CMakeLists.txt` already guards `add_subdirectory(tests)`/`add_subdirectory(bench)` behind an `is_toplevel` check (`get_directory_property(... PARENT_DIRECTORY)`), which is false when pulled in via `FetchContent` -- confirmed at configure time (only "Configuring chowdsp_wdf library..." appears, no test/bench targets), so letting its own script run normally is safe and simpler than NAM's glob-and-build-it-ourselves approach | Reimplementing NAM's `SOURCE_SUBDIR`-plus-manual-glob pattern for a dependency that doesn't actually need it |
| Pinned to a specific commit (`43bcd29...`), not the repo's sole `v1.0.0` tag | That tag is from Dec 2022, ~3.5 years stale against a repo still receiving commits as of mid-2026 (checked via GitHub API before picking); a moving `main` branch would make the build non-reproducible -- an exact recent commit is the middle ground, same reasoning as this project's own Eigen pin | The stale `v1.0.0` tag, or tracking `main` unpinned |
| No amp/preamp `EffectProcessor` built yet as of the `chowdsp_wdf` vendoring commit -- this pass only wires up the dependency | User's own words: "posso ver seu plano de implementaçao antes?" (amp research) and then asked to send a real circuit for a specific stage before anything gets modelled -- building a generic/placeholder WDF circuit then would have meant redoing it once the real circuit arrived, the exact "half-finished implementation" pattern this project avoids. **SUPERSEDED below** once the first real circuit (the GGG positive-ground booster) arrived | Building a generic placeholder triode stage then, to be replaced once a real circuit was provided |
| `PositiveGroundBoosterProcessor` (2026-09-14, the first physically-modelled pedal) uses a genuine Ebers-Moll transistor (`EbersMollBJT.h`) + trapezoidal-discretized capacitor companion models (`TrapezoidalCapacitor.h`), combined via DIRECT NODAL ANALYSIS with 3D Newton-Raphson -- NOT `chowdsp_wdf`'s generic WDF/R-type tree, despite it being vendored for exactly this purpose | Full reasoning in `docs/circuits/PositiveGroundBooster.md`. Short version: researched Chowdhury-DSP's own near-identical BYOD `RangeBooster` first (GPL-3.0, read for technique only) and found they make the same choice for the same reason -- every passive part in a single-transistor circuit touches one of the transistor's 3 terminals directly, so there's no separate linear sub-network for a generic tree's composability to earn its keep. Direct nodal analysis uses the identical trapezoidal capacitor math a WDF capacitor uses internally, so it's equally accurate, just more auditable (the KCL equations can be checked by hand) | Deriving a from-scratch 2-port BJT R-type adaptor for `chowdsp_wdf`'s tree -- a much harder, less independently-verifiable undertaking for no real fidelity gain here (see the tube-preamp family in `docs/circuits/CircuitFamilies.md` for where a real WDF tree IS expected to earn its keep later) |
| The BJT solve is genuinely 3-terminal (base, emitter, AND collector each get their own Thevenin-equivalent network folded in), not simplified to treat any one terminal as an ideal non-loading source | Every terminal in this circuit has real dynamics (a resistor and/or capacitor companion model) -- simplifying any of them would be less faithful than necessary. Deliberately MORE rigorous than BYOD's own reference implementation, which treats its base node as non-loading -- justified by this project's explicit fidelity-over-ease priority | Following BYOD's simplification for consistency -- rejected because it trades away real fidelity for no practicality gain (the 3x3 Newton-Raphson solve is still standard, well-conditioned, real-time-cheap) |
| A real bug (sign error on the input-coupling capacitor's Thevenin history term, causing the transistor to settle at cutoff instead of active bias) was caught by a bias-point test asserting the ACTUAL solved terminal voltages, not just boundedness/no-NaN | A transistor stuck at cutoff is perfectly finite, stable, and silent-ish -- indistinguishable from "working" under a pure boundedness check. Every future circuit-modelled processor's tests should include a DC-operating-point sanity assertion (forward-biased junction voltage, non-pinned terminal voltages), not just stress/NaN tests | Trusting boundedness tests alone for a circuit-modelled processor (would not have caught this) |
| `PositiveGroundBoosterProcessor::getDebugBiasPoint()` is a small permanent (not temporary) public accessor exposing the last-solved terminal voltages | Exists specifically so tests can assert bias-point sanity (see above) -- without it, there'd be no way to verify the fix for the cutoff bug beyond indirect behavioral inference | Removing it as "just debug scaffolding" once the bug was fixed -- it's the mechanism the regression test needs to keep catching this class of bug |
| `PositiveGroundBoosterProcessor::prepare()` (2026-09-14) runs the processor on ~1 second of silence via its own `process()` before returning, on top of the Newton-Raphson warm-start guess already there | User-reported: an audible pop/thump the instant this effect was added to a live chain. Root cause: the warm-start only seeded the transistor's iterative guess -- the capacitor histories still started at literal 0V, so real audio arrived mid-convergence. Reuses the already-verified `process()` path itself (same technique BYOD's own near-identical circuit uses) rather than deriving a separate closed-form DC formula -- safer, since it can't introduce a NEW derivation bug the way the cutoff bug above did | A closed-form analytical DC steady-state calculation -- would need re-deriving the discretized system's own fixed point (subtly different from the idealized "capacitor is open at DC" statement, since the discretized companion model's history/current terms only cancel out exactly AT convergence, not by construction), risking a second sign-derivation bug for a problem `process()` already solves correctly |
| Verified (2026-09-14): `EbersMollBJT`'s 3D Newton-Raphson solve runs at ~40x real-time on this project's dev machine (measured, not estimated -- see `Tests/PositiveGroundBoosterProcessorTests.cpp`'s CPU-cost test), ruling it out as the cause of a separately-reported general "latency" complaint | User reported audio latency around the same time as several other bugs; before assuming this expensive-looking per-sample solve was the cause, it was actually measured rather than guessed at | Assuming the obviously-most-expensive-looking new code was the culprit without measuring it first |
| `getName()` (the display name, everywhere the user actually sees it -- chain block, parameter panel, Add-effect menu) returns "Rangemaster-Style Booster", not "Rangemaster" or "Positive Ground Booster". The C++ class name (`PositiveGroundBoosterProcessor`), its registry key, and `docs/circuits/PositiveGroundBooster.md`'s own filename/title keep the more literal/descriptive name (internal/technical, not user-facing) | User initially wanted the literal name "Rangemaster" ("é um Rangemaster"). Rangemaster™ is a real, active trademark (Dallas Rangemaster → Dunlop) -- flagged that going open source doesn't remove this (copyright/license and trademark are unrelated legal concepts; being open source never grants permission to use someone else's brand name). Nominative fair use covers DESCRIBING/comparing to a trademark ("Rangemaster-style", "inspired by") but not BRANDING your own shipped feature with it -- "-Style" is the standard convention across the pedal-clone industry for exactly this reason, confirmed as the user's own preference once explained. **Standing convention for every future circuit-modelled pedal/amp**: default to "<OriginalName>-Style <Category>" rather than the bare original name, unless there's a specific reason a given circuit is different (e.g. it's based on a generic/unbranded circuit with no trademark at all) | Using "Rangemaster" as the literal display name (real trademark exposure for a product intended to ship); renaming the C++ class/registry key/doc filename too (those are internal identifiers, not user-facing, so the trademark concern doesn't apply to them, and renaming them would just be pure churn) |

## Circuit-Modeling Convention

Every physically-modelled (non-neural) pedal/amp gets a `docs/circuits/<Name>.md` writeup (topology, why that modelling approach, verified bias point/behaviour, explicit assumptions) plus an entry in `docs/circuits/CircuitFamilies.md` grouping it with related circuits. Check both, and the closest related existing circuit's own doc, before starting a new one -- see the Entry Points table above.

## Contracts
- Every effect class implements the *full* `EffectProcessor` contract — no partial exceptions (see root AGENTS.md).
- `process()` never allocates, blocks, or does I/O — model/IR loading always happens in `prepare()`/`loadModel()`/`loadImpulseResponse()` on the control thread, then handed to the audio thread via `DeferredReclaimer`.
- `nam::DSP::process()` output saturates to exactly `1.0f` in float32 at extreme drive — a bound test must use `<=`, not `<` (real bug, commit `dc090fe`).
- `juce::AudioProcessorParameterGroup`'s constructor is variadic-template (each `unique_ptr<Parameter>` as a separate argument), not a vector-taking constructor.
- `format` is the load-bearing TONE3000 field, not `gear`: `"nam"` → `NAMProcessor` only, `"ir"` → `IRLoaderProcessor` only. Mixing these up is a routing bug (see `Source/Tone3000/AGENTS.md`), not a matter of taste.

## Patterns

### Adding a new effect processor
1. Subclass `EffectProcessor`, implement the full contract (`prepare`/`process`/`reset`/`getParameters`/`getName`; override `getState`/`setState` only if state is more than float params).
2. Register it in `EffectRegistry::registerBuiltInEffects` (`Source/EffectRegistry.cpp`) — this is the one place concrete types are known.
3. Add its glyph as an `Assets/Icons/*.svg` matching the shared reference sheet, embed it via `CMakeLists.txt`'s `OpenGuitarMultiFx_Icons` target, draw it through `IconKit.h` in `drawIcon()`, set `getAccentColour()`, and add a row to `docs/icons/AGENT-icon-notes.md` — don't invent an ad-hoc glyph or hand-write `juce::Path` calls (see `docs/icons/AGENT-icon-notes.md`'s "To add a new icon" and root AGENTS.md's UI/UX Design Philosophy).
4. If it needs a loaded file (model/IR), override `wantsModelFile()`→true and `loadModelFile()`, and add a route in `Source/Tone3000/GearRouting.h` if it should be reachable via TONE3000 search.

## Pitfalls
- `EffectBlockComponent::paint()` used to hand `drawIcon()` a *stretched* (non-square) rectangle because width/height were reduced by different factors on a non-square block — every icon lacking a defensive `jmin(width,height)` came out visibly squashed until fixed in commit `1dbdc21`. New `drawIcon()` overrides can rely on the caller now passing a proper centred square, but don't assume every historical caller does.
- "Neural Amp" and "Neural Amp + Cab" process *identically* (same `.nam` file → `nam::get_dsp()`) — they look like they should be different code paths but are the exact same one; the split exists only for TONE3000 search scoping.
- Renaming a chain-role display name (e.g. the "NAM Amp"/"Neural Drive" → "Neural Amp"/"Neural Pedal" rename in commit `8d4316a`) requires updating every place that pattern-matches on the old name in lockstep — `GearRouting.h` and `MainComponent` both string-match on `getName()`; these are not meant to drift independently.

## Boundaries

### Always
- Implement the full `EffectProcessor` contract on every new subclass.
- Route new NAM/IR file types through `GearRouting.h` if TONE3000-downloadable.

### Never
- Add effect-type-specific special cases to `SignalGraph` — it must stay ignorant of concrete types.
- Call `loadModel()`/`loadImpulseResponse()` from the audio thread — control thread only, both mutate in place.
